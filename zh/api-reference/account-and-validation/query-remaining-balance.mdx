---
title: "查询剩余点数"
sidebarTitle: "查询剩余点数"
description: "查询后续操作可用的剩余点数。"
api: "POST https://alb.neural4d.com:3000/api/queryPointsInfo"
playground: simple
authMethod: bearer
---

## 授权

<ParamField header="Authorization" type="string" required default={'Bearer ${YOUR_API_KEY}'}>
  使用 Neural4D 网站提供的 Bearer token。
</ParamField>

## 请求体

<RequestExample>
```bash cURL
curl --request POST \
  --url https://alb.neural4d.com:3000/api/queryPointsInfo \
  --header 'Authorization: Bearer <token>'
```

```python Python
import requests

response = requests.request(
    "POST",
    "https://alb.neural4d.com:3000/api/queryPointsInfo",
    headers={
    "Authorization": "Bearer <token>",
    },
)
print(response.json())
```

```javascript JavaScript
const response = await fetch("https://alb.neural4d.com:3000/api/queryPointsInfo", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <token>"
  },
});
const data = await response.json();
console.log(data);
```

```typescript TypeScript
const response: Response = await fetch("https://alb.neural4d.com:3000/api/queryPointsInfo", {
  method: "POST",
  headers: {
    "Authorization": "Bearer <token>"
  },
});
const data = await response.json();
console.log(data);
```

```java Java
import java.net.URI;
import java.net.http.*;

HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://alb.neural4d.com:3000/api/queryPointsInfo"))
    .header("Authorization", "Bearer <token>")
    .method("POST", HttpRequest.BodyPublishers.noBody())
    .build();
HttpResponse<String> response = HttpClient.newHttpClient().send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

```go Go
package main

import (
  "fmt"
  "io"
  "net/http"
  "strings"
)

payload := nil
req, _ := http.NewRequest("POST", "https://alb.neural4d.com:3000/api/queryPointsInfo", payload)
req.Header.Set("Authorization", "Bearer <token>")
resp, _ := http.DefaultClient.Do(req)
defer resp.Body.Close()
bodyBytes, _ := io.ReadAll(resp.Body)
fmt.Println(string(bodyBytes))
```

```ruby Ruby
require "net/http"
require "json"

uri = URI("https://alb.neural4d.com:3000/api/queryPointsInfo")
request = Net::HTTP::Post.new(uri)
request["Authorization"] = "Bearer <token>"

response = Net::HTTP.start(uri.hostname, uri.port, use_ssl: uri.scheme == "https") { |http| http.request(request) }
puts response.body
```

```php PHP
<?php

$ch = curl_init("https://alb.neural4d.com:3000/api/queryPointsInfo");
curl_setopt_array($ch, [
    CURLOPT_CUSTOMREQUEST => "POST",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER => ["Authorization: Bearer <token>"],
]);
$response = curl_exec($ch);
curl_close($ch);
echo $response;
```

```csharp C#
using System.Net.Http;
using System.Text;

using var client = new HttpClient();
using var request = new HttpRequestMessage(HttpMethod.Post, "https://alb.neural4d.com:3000/api/queryPointsInfo");
request.Headers.TryAddWithoutValidation("Authorization", "Bearer <token>");
using var response = await client.SendAsync(request);
Console.WriteLine(await response.Content.ReadAsStringAsync());
```
</RequestExample>

## 响应

<Tabs sync={false}>
  <Tab title="200">
    剩余点数信息
        
    <ResponseField name="statusType" type="enum&lt;integer&gt;" required>
      查询状态。`0` 表示结果有效，`-1` 表示结果无效。

    可选值：`-1`、`0`。
    </ResponseField>

    <ResponseField name="data" type="object" required>
      按点数类型分组的当前余额。

      <Expandable title="child attributes">
        <ResponseField name="total_points" pre={["data."]} type="integer" required>
          账户当前可用的总点数。
        </ResponseField>

        <ResponseField name="monthly_bonus_points" pre={["data."]} type="integer" required>
          月度套餐赠送的可用点数。
        </ResponseField>

        <ResponseField name="permanent_points" pre={["data."]} type="integer" required>
          不按月到期的永久可用点数。
        </ResponseField>

        <ResponseField name="year_points" pre={["data."]} type="integer" required>
          年度套餐赠送的可用点数。
        </ResponseField>

        <ResponseField name="expiration_day" pre={["data."]} type="string | null" required>
          限时点数的到期日，存在时返回。
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="message" type="string" required>
      可读的点数余额查询结果。
    </ResponseField>
  </Tab>
</Tabs>

<ResponseExample>
```json 200 success
{
  "statusType": 0,
  "data": {
    "total_points": 54335,
    "monthly_bonus_points": 0,
    "permanent_points": 54335,
    "year_points": 0,
    "expiration_day": null
  },
  "message": "Get the points info successfully"
}
```

</ResponseExample>
